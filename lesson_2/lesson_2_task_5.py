original_reiting = [4, 2, 1]
new_el_reiting = int(input("Enter an integer ")

for el, i in enumerate(original_reiting)
    if el <= new_el_reiting
        original_reiting.insert(new_el_reiting, i)

print(original_reiting)

#не могу найти ошибку:(



